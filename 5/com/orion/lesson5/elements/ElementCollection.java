/*
package com.orion.lesson5.elements;

import com.orion.lesson5.UI;

import java.util.Map;

public enum ElementCollection {

    BUTTON {
        public Element getElement(Map params, String caption, UI scene){
            return new Button(params.get("x"), params.get("y"), params.get("weight"), params.get("height"), "Случайный элемент", new AddElementOnClick(scene));
        }
    },

    CHECK_BOS {
        public Element getElement (Map params) {
            return new CheckBox();
        }
    },

    TEXT_FIELD {
        public Element getElement (Map params) {
            return new TextField();
        }
    };

    public abstract Element getElement(Map params);

}
*/
